/**
 * Opening clearance rules - minimum distances around windows and doors.
 *
 * Intended purpose:
 * Returns clearance constraints for openings: minimum distance from wall edges,
 * exclusion zone around door, minimum gap between windows, and any other
 * spacing rules needed for clamp/validation in Window.jsx and wall logic.
 * Will support Bramwood rules and edge-case handling.
 */

export function getOpeningClearance() {
  // Placeholder - to be implemented when consolidating window/door logic.
}
