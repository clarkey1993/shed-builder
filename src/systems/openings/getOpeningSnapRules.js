/**
 * Snap rules for window and door openings.
 *
 * Intended purpose:
 * Returns snapping configuration: 6" grid cell size, 24" stud spacing,
 * stud-assist distance (e.g. 3"), and any other snap thresholds.
 * Will centralize the logic currently inline in Window.jsx clampAndSnap().
 */

export function getOpeningSnapRules() {
  // Placeholder - to be implemented when consolidating snapping logic.
}
